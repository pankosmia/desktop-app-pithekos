# desktop-app-pithekos
The Xenizo app based on Pankosmia components, with a focus on gateway languages and juxtalinears.

For linux:
```
# For each client repo in buildSpec.json:
# - clone
# - npm install
# - npm run build

cd linux/scripts
./build_and_run.bsh # Will build local server if there is no target dir
```